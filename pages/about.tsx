import { Box } from '@mui/material'
import * as React from 'react'

export interface AboutProps {}

export default function AboutPage(props: AboutProps) {
	return <Box sx={{ color: 'red' }}>AboutPage</Box>
}
